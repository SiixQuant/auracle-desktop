{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capabilities for the main Auracle Desktop window. Allow-listed plugins only — every privileged operation goes through a typed Tauri command, never a free-form shell or fs API.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "shell:default",
    "shell:allow-open",
    "dialog:default",
    "notification:default",
    "opener:default",
    "opener:allow-open-url",
    "store:default",
    "updater:default"
  ]
}
