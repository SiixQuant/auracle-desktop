// Tauri build script — generates compile-time artifacts (icon
// embedding, capability validation, OS-specific resource files).
// Standard boilerplate; do not edit unless adding a custom build
// step.

fn main() {
    tauri_build::build()
}
